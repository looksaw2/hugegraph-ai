1. 原文: _field_source 采用七类来源：
_field_source: dict[str, Literal[
    "yaml_persisted",
    "migrated_persisted",
    "explicit_persisted_patch",
    "effective_default",
    "dotenv_secret",
    "process_env_override",
    "runtime_only_patch",
]]
1. 意见: 建议明确 _field_source 的 key 统一格式。​推荐内部统一使用 global dotted leaf path，例如 llm.openai.chat.api_key；BaseConfig 对外可映射为 section-local flat field，例如 openai_chat_api_key。​否则 _flat_to_nested_mapping、_env_var_map、_mutable_persisted_fields、_field_source 四套映射容易各用各的 key，后续 save 过滤、patch 校验和 role 级联会出现错位。


2. 原文: 敏感字段分类原则：
2. 意见: 这里建议补一条 Sensitive YAML representation policy。​需要明确：YAML schema 是否允许出现 sensitive leaf path 的 null placeholder，例如 api_key: null、pwd: null。​推荐契约：示例文件可以保留 null 以提升可发现性；真实 save() 必须省略 sensitive path 或只写 null；update_config() 必须拒绝任何 sensitive path 的非空写入。这样能避免“字段 key 可见”和“secret value 落盘”被混淆。

3. 原文: Phase2 下 .env 的处理规则：
3. 意见: 建议把 Phase2 YAML + .env 共存场景补成决策矩阵。​例如：known sensitive key 参与 effective override；known non-sensitive legacy key 只 warning、不覆盖 YAML；unknown sensitive-like key 进入 report、不自动生效；unknown non-sensitive key 忽略或 report；process os.environ 始终最高优先级。​这样可以区分 legacy full .env 和 Phase2 secret-only .env，减少实现歧义。

4. 原文: - 只读配置目录下，如果无法创建或写入 config_prompt.yaml，启动 fail-fast。
4. 意见: 这里建议明确 strict vs fallback 策略。​只读配置目录下缺少 config_prompt.yaml 时直接 fail-fast 很严格，可能影响 K8s ConfigMap / 只读镜像部署。​建议评估 V1 是否允许 read-only fallback：缺少用户 prompt 文件且目录只读时，使用 package template 只读运行；只有用户显式保存 prompt 时才要求可写。若坚持 fail-fast，也需要把部署前置条件写进 hard gate。


5. 原文: 五、V1 目标范围与交付定义
5. 意见: 建议拆成本章的 V1 hard gate / V1.1-V2 follow-up。​V1 建议只保留核心闭环：配置加载与优先级、secret 不落盘、patch-only 写盘、no CWD fallback、基础迁移、必要测试 gate。​PromptConfig 全生命周期、API/operator 默认值全面审计、部署 grep gate、role 级联、完整回滚工具可以作为 V1.1/V2，避免 V1 范围过大导致实现不可控


6. 原文: migration_id 推荐格式：YYYYMMDDTHHMMSSZ；迁移报告推荐路径：<config_base_dir>/migration-reports/<migration_id>.md
6. 意见: 建议迁移报告同时提供 Markdown + JSON。​Markdown 适合人工审计，但自动回滚和测试需要机器可读状态。JSON 至少包含 migration_id、from_phase/to_phase、config_base_dir、created_files、backup_files、sensitive_keys_retained、persisted_keys、unknown_keys。​这样 rollback 命令和测试可以精确找到“本次迁移”对应的备份，而不是依赖文件名猜测。


7. 总体意见: 总体审计结论：方向正确，但 V1 范围需要收敛。​建议把 V1 定义为核心配置闭环：加载/合并优先级、secret 不落盘、patch-only 写盘、no CWD fallback、基础迁移和必要测试 gate。​目前文档把 PromptConfig、API/operator 默认值、部署文档、回滚、grep gate、role 级联等全部纳入 V1，工程风险偏高。建议拆成 V1 hard gate 与 V1.1/V2 follow-up，避免实现阶段范围失控。
 